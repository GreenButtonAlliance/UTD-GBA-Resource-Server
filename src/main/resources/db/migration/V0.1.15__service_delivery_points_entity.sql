CREATE TABLE IF NOT EXISTS usage.service_delivery_points (
                                                      uuid UUID PRIMARY KEY,
                                                      customerAgreement TEXT,
                                                      name TEXT,
                                                      tariffProfile TEXT
)
